# 📖 使用指南 —— 怎么把 AI 的回复推进收件箱

## 工作流全景

```
收（三选一） ──▶ 管（localhost:8787） ──▶ 做（一次只专注一条）
```

## 一、对 AI 说一句话（最常用）

适用于已接入 MCP 的工具：Claude Code / Codex / Antigravity / ZCode。
在它们的对话里直接说：

| 场景 | 话术示例 |
|---|---|
| 它给了一批建议，全要 | 「把这几条优化建议都记到我的收件箱」 |
| 只要某一条 | 「这条很好：给登录接口加限流。记到收件箱」 |
| 开工前接着干 | 「先读一下我的建议收件箱，我们继续上一条」 |
| 干完了 | 「把 #3 标记为完成」 |

> 来源工具、项目路径会自动记录，不需要你填。

## 二、选中文字捕获

- **任何 App（推荐，含 ZCode / WorkBuddy / TRAE / Antigravity / 终端）**：选中文字 → 按 **⌥S** → 右下角小窗 → ⏎ 入箱。配置见 HAMMERSPOON-SETUP.md。
  > ZCode 这类 Electron 应用右键是自己的菜单，不会出现系统「服务」，所以要用热键方式。
- **网页版对话**（ChatGPT/Claude/Gemini）：装好 extension 后，选中文字右键 →「📥 添加到建议收件箱」。卡片自带原对话链接。
- **原生 App**（TextEdit / Safari / 访达等）：也可以用 CAPTURE-SETUP.md 的快捷指令：选中文字右键 → 服务 → 添加到建议收件箱。

## 三、手动记

打开 http://localhost:8787 → 「＋ 收一条新内容」。适合突然想到的事。

## 四、拆步骤 & 做一步勾一步

收进来的卡片（尤其是一堆建议 / 安装操作步骤）有三种拆法：

| 方式 | 成本 | 适用 |
|---|---|---|
| 对 AI 说「把 #12 拆成步骤清单，命令标出来」 | 0（走 MCP） | 所有接了 MCP 的工具 |
| 卡片上点「✂ 按行拆步」 | 0 | 原文一行一步的清单 |
| 卡片上点「🪄 AI 拆步」 | 你的 API Key | 任意长文，右上角 ⚙️ 里配置（智谱/DeepSeek/OpenAI/自定义） |

拆好后点「开始专注」，今日专注框里直接是步骤清单：
**做完一步点 ☐ 打勾，命令点 ⧉ 随时再复制**——不用回聊天记录翻上下文。全部勾完点「✓ 整卡完成」。

不着急的建议点 ⚡ 闲时任务抽屉放进去，听到额度要重置了，就打开那个抽屉把剩余额度烧完。

## 日常循环（建议坚持两周养成习惯）

1. **随手收**：对话里觉得"这个好"，立刻说一句或右键一下
2. **抽空理**：每天打开收件箱扫一遍，垃圾删掉，重要的标待办
3. **专注做**：点「开始专注」，关掉其他念头，做完这一条再说
4. **打勾走人**：「✓ 完成」，自动回到下一条

## 常见问题

- **网页打不开？** 服务开机自启；万一掉了运行 `launchctl kickstart -k gui/$(id -u)/com.suggestion-inbox.server`
- **AI 说没有这个工具？** 重启对应工具；确认按 MCP-SETUP.md 注册过
- **ZCode 显示未连接？** 重启 ZCode / 重新打开项目（路径空格问题已修复）
- **数据会丢吗？** 全部在本地 data/inbox.db，备份这个文件即可
